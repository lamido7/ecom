import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import Admin from './Admin';

const AI = ReactDOM.createRoot(document.getElementById('root'));
AI.render(
  <Admin/>
);

export default AI